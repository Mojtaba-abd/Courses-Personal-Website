# MongoDB Connection String
# Required: Set either URL or MONGODB_URI (both work)
# 
# For local MongoDB without authentication:
# URL=mongodb://localhost:27017/lms-database
#
# For local MongoDB with authentication:
# URL=mongodb://username:password@localhost:27017/lms-database
#
# For MongoDB Atlas (cloud):
# URL=mongodb+srv://username:password@cluster.mongodb.net/database
#
# Note: If your password contains special characters, URL-encode them
# Example: @ becomes %40, # becomes %23, etc.
URL=mongodb://localhost:27017/lms-database
# MONGODB_URI=mongodb://localhost:27017/lms-database

# Server Port
# Optional: Defaults to 8000 if not set
PORT=8000

# JWT Secret Key
# Required: Change this to a secure random string in production!
# This secret is used to sign and verify JWT tokens for authentication
JWT_SECRET=your-secret-key-change-in-production

# JWT Token Expiration
# Optional: Defaults to "7d" (7 days) if not set
# Examples: "1h", "24h", "7d", "30d"
JWT_EXPIRES_IN=7d

# Gmail Email Configuration (Optional)
# Required if you want to use email features (contact form, course access requests)
# Get App Password from: https://myaccount.google.com/apppasswords
GMAIL_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-character-app-password

# Frontend URL (Optional)
# Used for CORS configuration
# Defaults allow localhost:3000 and localhost:3001
FRONTEND_URL=http://localhost:3000

