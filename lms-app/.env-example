# Backend API URL
# Required: The URL of your backend server
# For local development: http://localhost:8000
# For production: https://your-backend-domain.com
NEXT_PUBLIC_BACK_END_URL=http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Backend URL (Server-side only)
# Required for Next.js API routes that proxy to backend
# Should match NEXT_PUBLIC_BACK_END_URL
BACK_END_URL=http://localhost:8000

# JWT Secret Key
# Required: Must match the JWT_SECRET in backend .env
# This is used for server-side JWT verification in Next.js API routes
JWT_SECRET=your-secret-key-change-in-production

# Stripe Configuration (Optional)
# Required only if you want to use payment features
# Get your keys from: https://dashboard.stripe.com/apikeys
STRIPE_API_KEY=sk_test_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Mux Video Configuration (Optional)
# Required only if you want to use Mux for video hosting
# Get your tokens from: https://dashboard.mux.com/settings/api-credentials
MUX_TOKEN_ID=your_mux_token_id
MUX_TOKEN_SECRET=your_mux_token_secret

